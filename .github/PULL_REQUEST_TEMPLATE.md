## Summary

<!-- What changes, and why. Link the issue it closes if there is one. -->

## Verification

<!-- How you proved this works. Name the tests or harness probes you added or ran. -->

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass
- [ ] `cargo fmt --check`, `cargo clippy -- -D warnings`, and `cargo test` pass for both crates if Rust changed
- [ ] Tests or harness probes cover the change
- [ ] A changeset is included, or the change is not user-visible
- [ ] README, `docs/`, and the CHANGELOG are updated if the supported surface changed
